node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env
.env.local
.env.production
.vite/
scratch/

# Ignore built JS/CSS at root level (dev artifacts)
/*.js
/*.cjs
/*.mjs

# Keep public_html/ fully tracked (Hostinger deployment target)
# public_html/ is intentionally NOT ignored

# Keep static/ tracked (Vite publicDir source)
# static/ is intentionally NOT ignored

# Ignore /public (duplicate of public_html, not needed in git)
/public/
